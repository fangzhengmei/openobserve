# 查询结果缓存与分页协作机制全面分析

## 一、缓存键构建机制

### 1.1 缓存键层级结构

缓存键采用分层设计，确保不同维度的查询能够正确隔离：

**基础路径格式** (`cache/mod.rs:622-629`):
```
{org_id}/{stream_type}/{stream_name}/{hashed_query}
```

**缓存文件命名格式** (`cache/cacher.rs:452-458`):
```
{start_time}_{end_time}_{is_aggregate}_{is_descending}.json
```

### 1.2 哈希计算维度

查询哈希在 `prepare_cache_response` 函数中计算 (`cache/mod.rs:574-593`)，包含以下维度：

| 维度 | 说明 |
|------|------|
| **缓存版本** | `CACHE_VERSION = "v3"`，用于缓存格式升级时批量失效 |
| **SQL语句** | 规范化后的SQL查询语句 |
| **分页大小** | `req.query.size`，即每页返回的记录数 |
| **VRL函数** | 查询时使用的VRL转换函数（如果有） |
| **Action ID** | 动作标识符（如果有） |
| **区域信息** | `req.regions`，多区域查询时的区域列表 |
| **集群信息** | `req.clusters`，多集群查询时的集群列表 |

### 1.3 Histogram查询的特殊处理

对于聚合查询中的histogram，缓存键会额外附加信息 (`cache/cacher.rs:690-705`):

```rust
if is_aggregate && let Some(interval) = histogram_interval {
    file_path = format!("{file_path}_{interval}_{ts_column}");
}
```

这确保了不同时间间隔或不同时间列的histogram查询使用独立的缓存。

### 1.4 SQL规范化

在计算哈希前，会对histogram查询进行规范化处理 (`cache/mod.rs:556-572`):
- 统一时间间隔格式
- 确保相同语义的查询生成相同的哈希值
- 避免因时间间隔写法不同导致缓存不命中

---

## 二、分页协作机制

### 2.1 缓存启用条件

**关键限制** (`cache/mod.rs:92-98`):
```rust
let use_cache = if in_req.query.from == 0 {
    in_req.use_cache
} else {
    false
};
```

**设计说明**:
- 只有第一页查询 (`from == 0`) 才会使用结果缓存
- 翻页查询 (`from > 0`) 直接绕过缓存，实时查询
- 这是为了避免缓存数据与实时数据不一致导致分页重复或遗漏

### 2.2 增量更新机制

当缓存部分命中时，系统采用增量查询策略，只查询未覆盖的时间范围。

#### 2.2.1 单缓存增量计算 (`cache/cacher.rs:522-557`)

```rust
pub fn calculate_deltas(
    result_meta: &ResultCacheMeta,
    query_start_time: i64,
    query_end_time: i64,
    histogram_interval: i64,
    deltas: &mut Vec<QueryDelta>,
)
```

**计算逻辑**:
1. 如果查询时间范围与缓存完全重合，返回空delta
2. 如果查询结束时间晚于缓存结束时间，生成右侧delta
3. 如果查询开始时间早于缓存开始时间，生成左侧delta
4. 非histogram查询在delta起始时间+1微秒，避免重复数据

#### 2.2.2 多缓存增量计算 (`cache/cacher.rs:950-1005`)

```rust
fn calculate_deltas_multi(
    results: &[CachedQueryResponse],
    start_time: i64,
    end_time: i64,
    is_aggregate: bool,
    is_descending: bool,
    histogram_interval: i64,
) -> (Vec<QueryDelta>, Option<i64>, i64)
```

**多缓存协作**:
1. 按结束时间降序排列所有缓存片段
2. 逐个检查缓存片段，计算相邻片段间的gap
3. 合并重叠时间范围，去除重复delta
4. 最终生成最小化的查询时间范围集合

### 2.3 缓存选择策略

当存在多个缓存片段时，通过 `select_cache_meta` 函数选择最优缓存 (`cache/cacher.rs:772-796`):

| 策略 | 计算方式 | 适用场景 |
|------|----------|----------|
| **Overlap** | 缓存与查询的重叠时间长度 | 优先选择覆盖查询范围最多的缓存 |
| **Duration** | 缓存自身的时间跨度 | 优先选择时间范围最长的缓存 |
| **Both** | (重叠时长 / 缓存总时长) * 100 | 平衡重叠度和缓存效率，默认策略 |

### 2.4 结果合并流程

在 `merge_response` 函数中完成缓存与实时查询结果的合并 (`cache/mod.rs:667-828`):

1. **数据聚合**: 合并缓存结果和实时查询结果的hits
2. **统计累加**: 累加scan_size、scan_records等统计信息
3. **排序**: 根据ORDER BY字段对合并后的结果排序
4. **截断**: 如果结果数超过limit，截断到指定数量
5. **缓存率计算**: `result_cache_ratio = (缓存命中数 / 总结果数) * 100`

---

## 三、缓存失效机制

### 3.1 基于数据保留期的失效

`invalidate_cached_response_by_stream_min_ts` 函数确保不会返回已被清理的数据 (`cache/cacher.rs:46-112`):

#### 3.1.1 普通查询失效
```rust
responses
    .iter()
    .filter(|meta| meta.response_end_time >= stream_min_ts)
    .cloned()
    .map(|mut meta| {
        if meta.response_start_time < stream_min_ts {
            meta.response_start_time = stream_min_ts;
        }
        meta
    })
    .collect()
```

#### 3.1.2 Histogram查询失效
```rust
meta.cached_response.hits.retain(|hit| {
    let hit_ts = get_ts_value(&meta.ts_column, hit);
    hit_ts + histogram_interval > stream_min_ts
});
```
- 只有当整个bucket都过期时才删除
- 避免因部分过期导致delta计算错误

### 3.2 主动删除机制

`delete_cache` 函数支持三种删除模式 (`cache/cacher.rs:838-908`):

```rust
enum DeletionCriteria {
    TimeRange(i64, i64),    // 删除指定时间范围内的缓存
    ThresholdTimestamp(i64), // 删除早于指定时间的缓存
    DeleteAll,               // 删除所有缓存
}
```

**删除流程**:
1. 扫描缓存目录，解析文件名中的时间戳
2. 根据删除条件筛选需要删除的文件
3. 删除磁盘文件
4. 更新内存中的 `QUERY_RESULT_CACHE` 元数据
5. 同时删除结果缓存和聚合缓存（企业版）

### 3.3 缓存写入时的过滤

在 `write_results` 函数中，写入缓存前会进行多重过滤 (`cache/mod.rs:933-1060`):

1. **时间范围对齐**: histogram查询的时间范围必须是间隔的整数倍
2. **延迟写入**: 只缓存 `当前时间 - cache_delay_secs` 之前的数据，避免写入正在变化的数据
3. **最小时间范围**: 时间范围小于 `cache_delay_secs` 的不缓存
4. **空结果跳过**: 没有数据的结果不缓存

### 3.4 内存缓存淘汰

`TantivyResultCache` 使用FIFO队列进行内存管理 (`grpc/tantivy_result_cache.rs:114-143`):

```rust
if w.len() >= self.max_entries {
    // 释放10%的缓存条目
    for _ in 0..(std::cmp::max(1, self.max_entries / 10)) {
        if let Some(k) = w.pop_front() {
            self.readers.remove(&k);
        }
    }
}
w.push_back(key.clone());
```

---

## 四、租户隔离策略

### 4.1 路径级隔离

缓存键的最顶层就是租户ID，从根本上确保数据隔离:

**文件路径结构**:
```
results/{org_id}/{stream_type}/{stream_name}/{hashed_query}/{timestamp_files}.json
```

**内存缓存键**:
```rust
let query_key = file_path.replace('/', "_");
// org_id_streamtype_streamname_hashkey
```

### 4.2 集群级缓存失效

`delete_cached_results` 确保缓存删除操作在整个集群生效 (`cluster/cacher.rs:28-173`):

1. 获取所有在线的querier节点
2. 本地节点执行删除
3. 通过gRPC调用远程节点执行删除
4. 任意节点失败都会标记整体操作失败

### 4.3 工作队列隔离

企业版支持按租户进行并发控制 (`work_group.rs:100-199`):

- **三级并发控制**: 全局、租户、用户
- 每个租户有独立的并发配额
- 超过配额的查询进入排队等待

---

## 五、高并发降级处理策略

### 5.1 查询排队机制

#### 5.1.1 OSS版本 - 全局队列 (`work_group.rs:48-91`)
```rust
let locker_key = format!("/search/cluster_queue/{work_group_str}");
let locker = infra::dist_lock::lock_with_trace_id(trace_id, &locker_key, timeout)
    .await
    .map_err(|e| {
        metrics::QUERY_PENDING_NUMS
            .with_label_values(&[org_id])
            .dec();
        Error::Message(e.to_string())
    })?;
```

- 使用分布式锁实现全局并发控制
- 获取锁失败则查询失败，避免系统过载
- 通过 `QUERY_PENDING_NUMS` 指标监控排队情况

#### 5.1.2 企业版本 - 工作组队列 (`work_group.rs:100-199`)

工作组支持更精细的并发控制:
1. **预测工作组**: 根据SQL复杂度预测合适的工作组
2. **三级检查**: 同时检查全局、租户、用户三个级别的并发数
3. **等待机制**: 支持排队等待，而不是直接拒绝
4. **超时控制**: 等待超过timeout则失败

### 5.2 部分结果返回

当查询出现部分失败时，系统支持返回已获取的结果:

**部分响应类型** (`cache/mod.rs:429-432`):
1. VRL函数执行错误
2. 超集群节点错误
3. 查询时间范围超过限制

**标记方式**:
- `is_partial = true` 标记结果不完整
- `function_error` 字段记录具体错误信息
- `new_start_time` 和 `new_end_time` 标记实际查询的时间范围

### 5.3 内存资源控制

`TantivyResultCache` 严格控制内存使用 (`grpc/tantivy_result_cache.rs:63-90`):

```rust
pub fn get_memory_size(&self) -> usize {
    match self {
        CacheEntry::RowIdsBitVec(_, bitvec) => {
            bitvec.capacity().div_ceil(8) + std::mem::size_of::<BitVec>()
        }
        CacheEntry::RowIdsRoaring(_, roaring, _) => {
            roaring.serialized_size() + ...
        }
        // ... 其他类型计算
    }
}
```

- 精确计算每种缓存类型的内存占用
- 超过 `max_entries` 时自动淘汰10%的旧条目
- 通过 `TANTIVY_RESULT_CACHE_MEMORY_USAGE` 指标监控

### 5.4 缓存开关配置

系统支持多级缓存控制:

| 配置项 | 作用 |
|--------|------|
| `result_cache_enabled` | 全局结果缓存开关 |
| `use_multi_result_cache` | 是否启用多级缓存 |
| `result_cache_selection_strategy` | 缓存选择策略 |
| `cache_delay_secs` | 缓存延迟写入时间 |
| `result_cache_max_entries` | 内存缓存最大条目数 |

### 5.5 并行查询控制

增量查询时，所有delta时间范围并行执行 (`cache/mod.rs:227-268`):

```rust
let mut tasks = Vec::new();
for (i, delta) in c_resp.deltas.into_iter().enumerate() {
    let task = tokio::task::spawn(async move {
        req.query.start_time = delta.delta_start_time;
        req.query.end_time = delta.delta_end_time;
        SearchService::search(&trace_id, &org_id, stream_type, user_id, &req).await
    });
    tasks.push(task);
}
```

- 利用tokio异步运行时实现真正的并行查询
- 所有delta查询完成后才进行结果合并
- 充分利用多核CPU资源

---

## 六、核心数据流

```
查询请求
    ↓
┌─────────────────────────────────────────┐
│ prepare_cache_response                  │
│ 1. 解析SQL，提取元数据                  │
│ 2. 规范化histogram时间间隔              │
│ 3. 计算查询哈希，构建缓存键             │
│ 4. 确定时间列和排序方向                 │
└─────────────────────────────────────────┘
    ↓
from == 0? ──否──→ 直接查询 ──→ 结果返回
    │是
    ↓
┌─────────────────────────────────────────┐
│ check_cache                             │
│ 1. 查询内存中的缓存元数据               │
│ 2. 按选择策略筛选最优缓存               │
│ 3. 基于stream_min_ts过滤过期缓存        │
│ 4. 计算需要增量查询的时间范围           │
└─────────────────────────────────────────┘
    ↓
完全命中? ──是──→ 返回缓存结果
    │否
    ↓
┌─────────────────────────────────────────┐
│ 并行增量查询                            │
│ 对每个delta时间范围发起并行查询         │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ merge_response                          │
│ 1. 合并缓存和实时查询结果               │
│ 2. 按ORDER BY排序                       │
│ 3. 截断到limit数量                      │
│ 4. 计算缓存命中率                       │
└─────────────────────────────────────────┘
    ↓
┌─────────────────────────────────────────┐
│ write_results                           │
│ 1. 过滤不稳定的数据                     │
│ 2. 异步写入磁盘缓存                     │
│ 3. 更新内存缓存元数据                   │
└─────────────────────────────────────────┘
    ↓
结果返回
```

---

## 七、关键代码文件索引

| 文件 | 核心功能 |
|------|----------|
| `src/service/search/cache/mod.rs` | 缓存主流程，包括准备缓存、搜索、合并、写入 |
| `src/service/search/cache/cacher.rs` | 缓存检查、delta计算、缓存选择、失效处理 |
| `src/service/search/cache/multi.rs` | 多级缓存实现，多缓存片段协作 |
| `src/service/search/cluster/cacher.rs` | 集群缓存删除，跨节点缓存失效 |
| `src/service/search/grpc/tantivy_result_cache.rs` | 内存倒排索引结果缓存 |
| `src/service/search/work_group.rs` | 并发控制、排队机制、降级处理 |
| `src/common/meta/search.rs` | 缓存相关数据结构定义 |
