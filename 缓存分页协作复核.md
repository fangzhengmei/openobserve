# 缓存与分页协作机制复核分析

## 一、从 prepare_cache_response 到 check_cache 的 ORDER BY 传递链

### 1.1 完整调用链概览

```
HTTP Handler (handler/http/request/search/mod.rs:486)
    ↓
SearchService::cache::search (cache/mod.rs:79)
    ├─ 步骤1: use_cache 判断 (cache/mod.rs:92-98)
    ├─ 步骤2: VRL函数预处理 (cache/mod.rs:102-118)
    ├─ 步骤3: prepare_cache_response (cache/mod.rs:121-122)
    │   ├─ 解析SQL, 提取order_by
    │   ├─ 规范化histogram (cache/mod.rs:555-572)
    │   ├─ 计算hashed_query (cache/mod.rs:574-593)
    │   ├─ 推断ts_column和is_descending (cache/cacher.rs:626-678)
    │   ├─ 构建file_path (cache/cacher.rs:683-705)
    │   └─ 分支判断 (cache/mod.rs:633-659)
    │       ├─ use_cache=true → 调用 check_cache
    │       └─ use_cache=false → 返回元数据, 不查缓存
    ├─ 步骤4: 执行delta查询 (cache/mod.rs:227-268)
    ├─ 步骤5: merge_response/sort_response (cache/mod.rs:667-899)
    └─ 步骤6: write_results (cache/mod.rs:435-474)
```

### 1.2 ORDER BY 在 SQL 文本中的位置

ORDER BY 是 SQL 语句的一部分，存储在 `req.query.sql` 中。

**Request 结构** (`config/src/meta/search.rs:55-148`):
```rust
pub struct Request {
    pub query: Query,
    // ...
}

pub struct Query {
    pub sql: String,      // 包含完整SQL, 包括ORDER BY
    pub start_time: i64,
    pub end_time: i64,
    pub from: i64,        // 分页偏移
    pub size: i64,        // 每页大小
    // ...
}
```

**SQL 示例**:
```sql
SELECT * FROM logs 
WHERE level = 'error' 
ORDER BY _timestamp DESC, service_name ASC 
LIMIT 100 OFFSET 0
```

### 1.3 SQL 解析与 ORDER BY 提取

在 `prepare_cache_response` 中 (`cache/mod.rs:497-661`):

```rust
pub async fn prepare_cache_response(...) -> Result<(MultiCachedQueryResponse, bool), Error> {
    // 1. 从req中获取原始SQL
    let mut origin_sql = req.query.sql.clone();
    
    // 2. 解析SQL, 提取ORDER BY
    let sql = match parse_sql(&origin_sql, None) {
        Ok(sql) => sql,
        Err(_) => return Ok((MultiCachedQueryResponse::default(), true)),
    };
    
    // sql.order_by 类型: Vec<(String, OrderBy)>
    // 例如: [("_timestamp", Desc), ("service_name", Asc)]
    let order_by = &sql.order_by;
```

**OrderBy 枚举定义**:
```rust
pub enum OrderBy {
    Asc,
    Desc,
}
```

### 1.4 时序列与排序方向推断

调用 `get_ts_col_order_by` 从 ORDER BY 中推断时序列和排序方向 (`cache/cacher.rs:626-678`):

```rust
pub fn get_ts_col_order_by(
    parsed_sql: &Sql,
    _ts_col: &str,
    _is_aggregate: bool,
) -> Option<(String, bool)> {
    let order_by = &parsed_sql.order_by;
    let mut result_ts_col = String::new();
    let mut is_descending = true;  // 默认降序
    
    if order_by.is_empty() {
        // 没有ORDER BY, 使用默认时序列和降序
        return Some((TIMESTAMP_COL_NAME.to_string(), true));
    }
    
    // 遍历ORDER BY字段, 找到时序列
    for (field, order) in order_by {
        if is_timestamp_field(field, &result_ts_col) {
            result_ts_col = field.clone();
            is_descending = order == &OrderBy::Desc;
            break;  // 只考虑第一个时序列的排序方向
        }
    }
    
    // 如果是聚合查询且ORDER BY不在时序列上
    if is_aggregate && order_by.is_empty() && result_ts_col.is_empty() {
        return None;  // 禁用缓存
    }
    
    Some((result_ts_col, is_descending))
}
```

**关键点**:
- 只有**第一个时序列**的排序方向会被提取为 `is_descending`
- 非时序列的排序字段（如按count排序）不影响 `is_descending`
- 对于histogram非时序排序，有专门的 `refine_is_descending_for_histogram` 处理

### 1.5 hashed_query 的计算

**哈希计算代码** (`cache/mod.rs:574-593`):

```rust
let mut hash_body = vec![
    CACHE_VERSION.to_string(),      // "v3"
    origin_sql.to_string(),         // ★ 包含ORDER BY的完整SQL字符串
    req.query.size.to_string(),     // 分页大小
];

if let Some(vrl_function) = &query_fn {
    hash_body.push(vrl_function.to_string());
}
if let Some(action_id) = action {
    hash_body.push(action_id.to_string());
}
if !req.regions.is_empty() {
    hash_body.extend(req.regions.clone());
}
if !req.clusters.is_empty() {
    hash_body.extend(req.clusters.clone());
}

// gxhash::gxhash64 计算哈希
let hashed_query = gxhash::gxhash64(
    hash_body.join("").as_bytes(),
    1234,
)
.to_string();
```

**重要结论**:
- ✅ **ORDER BY 完整进入 hashed_query**，因为它是 `origin_sql` 的一部分
- ✅ **WHERE 条件也进入 hashed_query**，同样是 `origin_sql` 的一部分
- ✅ **LIMIT / OFFSET 也进入 hashed_query**，因为在SQL字符串中
- ❌ **`req.query.from` 不直接参与哈希计算**，但如果SQL中有OFFSET则会在SQL中

### 1.6 缓存目录与文件命名

**缓存文件路径构建** (`cache/mod.rs:622-629`):

```rust
let base_file_path = format!("{org_id}/{stream_type}/{stream_name}/{hashed_query}");
let file_path = cacher::compute_cache_file_path(
    &base_file_path,
    is_aggregate,
    sql.histogram_interval,
    &ts_column,
);
```

**`compute_cache_file_path` 实现** (`cache/cacher.rs:683-705`):

```rust
pub fn compute_cache_file_path(
    base_file_path: &str,
    is_aggregate: bool,
    histogram_interval: i64,
    ts_column: &str,
) -> String {
    let mut file_path = base_file_path.to_string();
    
    // Histogram查询追加时间间隔和时间列
    if is_aggregate && histogram_interval > 0 {
        file_path = format!("{file_path}_{histogram_interval}_{ts_column}");
    }
    
    file_path
}
```

**缓存文件名** (`cache/cacher.rs:452-458`):
```rust
let file_name = format!(
    "{start_time}_{end_time}_{is_aggregate}_{is_descending}.json",
);
```

### 1.7 排序信息的三层传递总结

| 排序信息 | 传递方式 | 进入hashed_query? | 影响文件名? |
|---------|---------|-------------------|------------|
| **完整ORDER BY子句** | 作为SQL字符串的一部分参与哈希 | ✅ 是 | ❌ 否 |
| **is_descending** | 从ORDER BY推断出的布尔值 | ❌ 否（通过SQL隐含） | ✅ 是（文件名后缀） |
| **非时序排序列** | 在SQL中参与哈希，merge时重新排序 | ✅ 是（通过SQL） | ❌ 否 |
| **排序方向** | 在SQL中参与哈希，同时提取为is_descending | ✅ 是（通过SQL） | ✅ 是（文件名后缀） |

**示例说明**:

```
查询1: SELECT * FROM logs ORDER BY _timestamp DESC LIMIT 100
  origin_sql = "SELECT * FROM logs ORDER BY _timestamp DESC LIMIT 100"
  → hashed_query = hash("v3" + origin_sql + "100")
  → 缓存目录: org/default/logs/abc123
  → 文件名: 100000_200000_0_1.json  (1表示descending)

查询2: SELECT * FROM logs ORDER BY _timestamp ASC LIMIT 100
  origin_sql = "SELECT * FROM logs ORDER BY _timestamp ASC LIMIT 100"
  → hashed_query = hash(...)  ← 不同的哈希！
  → 缓存目录: org/default/logs/def456  ← 不同的目录！
  → 文件名: 100000_200000_0_0.json  (0表示ascending)

查询3: SELECT * FROM logs ORDER BY service_name ASC, _timestamp DESC LIMIT 100
  origin_sql = "SELECT * FROM logs ORDER BY service_name ASC, _timestamp DESC LIMIT 100"
  → hashed_query = hash(...)  ← 不同的哈希！
  → 缓存目录: org/default/logs/ghi789  ← 不同的目录！
  → is_descending = true (从第一个时序列推断)
  → 文件名: 100000_200000_0_1.json
```

**为什么 is_descending 既要在SQL中又要在文件名中？**

1. **SQL中的ORDER BY**：确保不同排序是不同的查询，使用不同缓存目录
2. **文件名中的is_descending**：
   - 同一目录下可能有不同排序方向的缓存文件？
   - 不，实际上SQL不同，目录就不同
   - 文件名的is_descending更多是为了快速筛选，无需打开文件就能知道排序方向
   - 增量查询时需要知道缓存数据的排序方向来计算delta

---

## 二、时间窗变化时的缓存命中与失效

### 2.1 哈希键与文件名的协作机制

```
相同SQL + 不同时间窗
    ↓
hashed_query 相同 → 进入同一缓存目录
    ↓
目录下有多个文件，每个代表一个时间窗:
    100000_200000_0_1.json   ← 10:00-11:00
    200000_300000_0_1.json   ← 11:00-12:00
    150000_250000_0_1.json   ← 10:30-11:30
```

### 2.2 check_cache 中的缓存筛选

`get_cached_results` 函数 (`cache/cacher.rs:415-449`):

```rust
pub fn get_cached_results(...) -> Option<CachedQueryResponse> {
    // 1. 从内存中获取该目录下所有缓存元数据
    let cache_metas = QUERY_RESULT_CACHE.get(&query_key)?;
    
    // 2. 筛选时间范围重叠的缓存
    let mut matching_meta = cache_metas
        .iter()
        .filter(|m| {
            // 时间范围有交集
            m.start_time <= cache_req.q_end_time 
            && m.end_time >= cache_req.q_start_time
            
            // Histogram额外检查: 时间边界对齐
            && if cache_req.is_aggregate && cache_req.histogram_interval > 0 {
                m.start_time % cache_req.histogram_interval == 0
                && m.end_time % cache_req.histogram_interval == 0
            } else {
                true
            }
        })
        // 3. 按选择策略打分, 选择最优缓存
        .max_by_key(|result| select_cache_meta(result, &cache_req, &selection_strategy))?;
```

### 2.3 时间窗扩大示例

```
查询1: 10:00-11:00, size=100, ORDER BY _timestamp DESC
  → hashed_query = abc123
  → 创建缓存文件: 100000_200000_0_1.json

查询2: 10:00-12:00, 相同SQL和size
  → hashed_query = abc123  (相同！)
  → 进入同一缓存目录
  → 找到缓存文件 100000_200000_0_1.json
  → 检查重叠: 100000 <= 300000 && 200000 >= 100000 = true
  → 计算delta: start = 200000 + 1 = 200001, end = 300000
  → 执行delta查询 [200001, 300000]
  → 合并缓存 [100000, 200000] + delta [200001, 300000]
  → 写入新缓存文件: 100000_300000_0_1.json (或保留两个文件)

现在目录下有:
    100000_200000_0_1.json
    100000_300000_0_1.json  ← 更大的时间范围

查询3: 09:00-12:00, 相同SQL
  → hashed_query = abc123
  → 有两个候选缓存:
    * 100000_200000_0_1.json → 覆盖1小时
    * 100000_300000_0_1.json → 覆盖2小时, 重叠更多
  → 选择 100000_300000_0_1.json
  → delta: [090000, 099999] + [300001, 360000]? 不, 是一个连续delta
  → 实际delta: [090000, 099999] (左侧) + [300001, 360000] (右侧)
```

### 2.4 多缓存片段协作

`calculate_deltas_multi` 处理多个缓存片段 (`cache/cacher.rs:950-1005`):

```rust
fn calculate_deltas_multi(
    results: &[CachedQueryResponse],
    start_time: i64,
    end_time: i64,
    ...
) -> (Vec<QueryDelta>, Option<i64>, i64) {
    // 按结束时间降序排列
    let mut results = results.to_vec();
    results.sort_by(|a, b| b.response_end_time.cmp(&a.response_end_time));
    
    let mut deltas = Vec::new();
    let mut current_end_time = end_time;
    
    for meta in results {
        // 处理当前缓存与current_end_time之间的gap
        calculate_deltas(
            &ResultCacheMeta {
                start_time: meta.response_start_time,
                end_time: meta.response_end_time,
            },
            meta.response_start_time,
            current_end_time,
            ...,
            &mut deltas,
        );
        current_end_time = meta.response_start_time;
    }
    
    // 处理最左侧的gap (查询开始到第一个缓存开始)
    if current_end_time > start_time {
        deltas.push(QueryDelta {
            delta_start_time: start_time,
            delta_end_time: current_end_time - 1,
        });
    }
    
    // 排序并去重
    deltas.sort();
    deltas.dedup();
    
    (deltas, min_ts, total_cache_duration)
}
```

### 2.5 缓存失效机制

#### 2.5.1 基于 stream_min_ts 的被动失效

`invalidate_cached_response_by_stream_min_ts` (`cache/cacher.rs:46-112`):

```rust
pub async fn invalidate_cached_response_by_stream_min_ts(
    file_path: &str,
    responses: &[CachedQueryResponse],
    histogram_interval: i64,
) -> Result<Vec<CachedQueryResponse>, String> {
    // 获取流中现存数据的最早时间戳
    let stream_min_ts = infra::cache::stats::get_stream_stats(...).doc_time_min;
    
    if histogram_interval > 0 {
        // Histogram查询: 只删除完全过期的bucket
        // bucket [10:00-11:00] 完全过期 <-> 11:00 <= stream_min_ts
        for meta in responses {
            meta.cached_response.hits.retain(|hit| {
                let hit_ts = get_ts_value(&meta.ts_column, hit);
                hit_ts + histogram_interval > stream_min_ts
            });
        }
    } else {
        // 普通查询: 过滤掉已过期的缓存, 修剪开始时间
        responses
            .iter()
            .filter(|meta| meta.response_end_time >= stream_min_ts)
            .map(|mut meta| {
                if meta.response_start_time < stream_min_ts {
                    meta.response_start_time = stream_min_ts;
                }
                meta
            })
            .collect()
    }
}
```

#### 2.5.2 主动删除

`delete_cache` 支持三种模式 (`cache/cacher.rs:838-908`):
- `TimeRange(start, end)`: 删除指定时间范围内的缓存
- `ThresholdTimestamp(ts)`: 删除早于ts的所有缓存
- `DeleteAll`: 删除该目录下所有缓存

#### 2.5.3 写入时的延迟过滤

`write_results` 只缓存稳定数据 (`cache/mod.rs:933-1060`):

```rust
// 只缓存早于 (当前时间 - cache_delay_secs) 的数据
let cache_latest_time = now_micros() - cfg.common.result_cache_max_delay_secs * 1_000_000;
if cache_latest_time <= req.query.start_time {
    return Ok(());  // 数据太新, 不缓存
}

// 实际缓存的结束时间取 min(查询结束时间, cache_latest_time)
let cache_end_time = std::cmp::min(req.query.end_time, cache_latest_time);
```

---

## 三、from > 0 绕过缓存与结果一致性

### 3.1 use_cache 判断的代码位置

**关键代码** (`cache/mod.rs:92-98`):

```rust
// result cache can be enable only when its from the start
let use_cache = if in_req.query.from == 0 {
    in_req.use_cache
} else {
    false  // from > 0 时强制禁用缓存
};
```

**注释明确说明**: "result cache can be enable only when its from the start"

### 3.2 from > 0 时的执行路径

当 `from > 0` 时，`prepare_cache_response` 的返回路径 (`cache/mod.rs:633-659`):

```rust
let resp = if use_cache {
    // use_cache=true (from=0): 检查缓存
    check_cache(...).await
} else {
    // use_cache=false (from>0): 只返回解析后的元数据, 不查缓存
    MultiCachedQueryResponse {
        ts_column,
        is_aggregate,
        is_descending,
        order_by: sql.order_by,  // 仍然解析ORDER BY用于后续排序
        limit: sql.limit,
        file_path,              // 仍然计算file_path, 但不会用于读写
        ..Default::default()    // cached_response = [], deltas = [], has_cached_data = false
    }
};
```

**后续执行** (`cache/mod.rs:136-142`):

```rust
// 没有缓存数据且deltas为空, 添加完整查询范围的delta
if !c_resp.has_cached_data && c_resp.deltas.is_empty() {
    c_resp.deltas.push(QueryDelta {
        delta_start_time: req.query.start_time,
        delta_end_time: req.query.end_time,
    });
}
```

### 3.3 首页与翻页的完整路径对比

#### 3.3.1 首页 (from=0, size=100)

```
请求参数: from=0, size=100
         sql="SELECT * FROM logs WHERE level='error' ORDER BY _timestamp DESC LIMIT 100"

执行路径:
1. use_cache = true
2. prepare_cache_response:
   - 解析SQL, order_by=[("_timestamp", Desc)]
   - 计算hashed_query = hash("v3" + sql + "100")
   - 调用check_cache:
     * 遍历缓存目录, 找到匹配的缓存文件
     * 计算delta时间范围
     * 可能是部分命中或完全命中
3. 执行delta查询 (如果需要)
4. merge_response:
   - 合并缓存和delta结果
   - 按ORDER BY重新排序
   - 截断到size=100条
5. write_results: 异步写入新缓存
6. 返回结果 (记录1-100)
```

#### 3.3.2 翻页 (from=100, size=100)

```
请求参数: from=100, size=100
         sql="SELECT * FROM logs WHERE level='error' ORDER BY _timestamp DESC LIMIT 100 OFFSET 100"

执行路径:
1. use_cache = false (因为 from=100 > 0)
2. prepare_cache_response:
   - 解析SQL, order_by=[("_timestamp", Desc)]
   - 计算hashed_query = hash("v3" + sql + "100") ← 注意: SQL中包含OFFSET 100, 哈希不同！
   - 不调用check_cache, 直接返回元数据
   - cached_response = [], has_cached_data = false
3. 添加完整delta: [start_time, end_time]
4. 执行完整实时查询 (不使用任何缓存)
5. sort_response:
   - 只有搜索结果, 没有缓存结果
   - 按ORDER BY排序
   - 结果在底层查询引擎中已应用OFFSET
6. 跳过 write_results (因为 use_cache=false 或 from>0)
7. 返回结果 (记录101-200, 纯实时数据)
```

### 3.4 结果一致性保证机制

**问题**: 首页用缓存+delta，翻页用纯实时查询，如何保证分页不重复不遗漏？

#### 3.4.1 机制1: SQL 中的 OFFSET 参与哈希

首页SQL: `SELECT ... LIMIT 100`
翻页SQL: `SELECT ... LIMIT 100 OFFSET 100`

→ **SQL字符串不同 → hashed_query不同 → 缓存目录不同**

这意味着翻页查询**永远不会**使用首页的缓存，从根本上避免了缓存与实时数据不一致导致的分页问题。

#### 3.4.2 机制2: 相同排序依据 + 相同数据源

虽然翻页绕过缓存，但排序依据是相同的：

1. **ORDER BY 子句相同**:
   ```rust
   // 首页:
   merge_response(..., c_resp.is_descending, c_resp.order_by, ...)
   
   // 翻页:
   sort_response(c_resp.is_descending, &mut reps, &c_resp.ts_column, &c_resp.order_by)
   ```

2. **最终排序逻辑相同** (`cache/mod.rs:830-899`):
   ```rust
   fn sort_response(is_descending: bool, cache_response: &mut Response, ts_column: &str, in_order_by: &Vec<(String, OrderBy)>) {
       let order_by = if in_order_by.is_empty() {
           &vec![(ts_column.to_string(), if is_descending {Desc} else {Asc})]
       } else {
           in_order_by
       };
       
       cache_response.hits.sort_by(|a, b| {
           for (field, order) in order_by {
               // 按字段逐一比较...
           }
       });
   }
   ```

#### 3.4.3 机制3: 底层查询引擎的 OFFSET 处理

当 `from > 0` 时，`req.query.from` 和 `req.query.size` 会传递给底层查询引擎（DataFusion/Tantivy），由引擎在查询时应用OFFSET。

**在 streaming execution 中** (`streaming/execution.rs:112-168`):
```rust
let original_from = req.query.from as usize;
let original_size = if req.query.size > 0 {
    req.query.size as usize
} else {
    usize::MAX
};

// heap大小 = from + size, 保证有足够的元素来跳过offset
let heap_k = if req.query.size > 0 {
    original_from + original_size
} else {
    usize::MAX
};

// 每个分区查询前 from+size 条
req.query.size = heap_k as i64;
req.query.from = 0;

// 合并后跳过前from条
let top_k_values = top_k_heap.into_sorted_vec(original_from);
```

#### 3.4.4 机制4: delta边界的 +1 微秒

虽然翻页不使用缓存，但首页的delta查询通过+1微秒避免边界重复：

```rust
// 普通查询: delta_start = cache_end + 1微秒
let delta_start_time = if histogram_interval > 0 {
    result_meta.end_time      // Histogram按bucket边界对齐
} else {
    result_meta.end_time + 1  // 普通查询+1微秒避免重复
};
```

**一致性说明**:
- 首页缓存的 `[start, cache_end]` 和 delta 的 `[cache_end+1, end]` 没有重叠
- 翻页查询整个 `[start, end]` 范围，由底层引擎处理OFFSET
- 由于数据是不可变的（immutable），已固化的数据不会变化
- 新写入的数据时间戳 > 当前时间，不会出现在历史查询结果中

#### 3.4.5 潜在的不一致场景及应对

| 场景 | 可能性 | 影响 | 应对方式 |
|-----|--------|------|---------|
| 首页查询后，有延迟数据写入到首页覆盖的时间范围 | 中 | 翻页时可能看到本应在首页的记录 | 设计上接受，因为延迟数据本身就是异常 |
| 首页使用了缓存，翻页使用实时查询，排序键值相同的记录顺序不同 | 低 | 分页边界可能重复或遗漏 | 增加二级排序键（如_id）保证稳定性 |
| 数据保留策略在两次查询之间清理了部分数据 | 低 | 分页偏移错位 | 数据清理通常按天/小时粒度，短时间内连续翻页不会触发 |
| 首页delta查询+1微秒，翻页没有+1 | 极低 | 边界时间戳的记录可能出现在两页 | +1微秒远小于通常的查询粒度（秒级），实际不会发生 |

---

## 四、完整调用链代码索引

### 4.1 ORDER BY 传递链

| 阶段 | 代码位置 | 关键变量 |
|-----|---------|---------|
| HTTP请求解析 | `handler/http/request/search/mod.rs:486` | `req.query.sql`, `req.query.from`, `req.query.size` |
| use_cache判断 | `cache/mod.rs:92-98` | `use_cache` |
| SQL解析 | `cache/mod.rs:505-540` | `sql.order_by` |
| 时序列推断 | `cache/cacher.rs:626-678` | `get_ts_col_order_by` → `(ts_column, is_descending)` |
| 哈希计算 | `cache/mod.rs:574-593` | `hashed_query` |
| 缓存路径构建 | `cache/mod.rs:622-629` | `file_path` |
| 分支选择 | `cache/mod.rs:633-659` | `use_cache` → check_cache 或 元数据返回 |
| 结果排序 | `cache/mod.rs:830-899` | `sort_response` |

### 4.2 关键函数签名

```rust
// 缓存搜索主入口
pub async fn search(
    trace_id: &str,
    org_id: &str,
    stream_type: StreamType,
    user_id: Option<String>,
    in_req: &search::Request,  // 包含 from, size, sql
    range_error: String,
    is_http2_streaming: bool,
    dashboard_info: Option<DashboardInfo>,
    is_multi_stream_search: bool,
) -> Result<search::Response, Error>

// 准备缓存响应
pub async fn prepare_cache_response(
    trace_id: &str,
    org_id: &str,
    stream_type: StreamType,
    req: &mut search::Request,
    use_cache: bool,
) -> Result<(MultiCachedQueryResponse, bool), Error>

// 从ORDER BY推断时序列
pub fn get_ts_col_order_by(
    parsed_sql: &Sql,
    _ts_col: &str,
    _is_aggregate: bool,
) -> Option<(String, bool)>  // (ts_column, is_descending)

// 检查缓存
pub async fn check_cache(...) -> MultiCachedQueryResponse

// 合并响应
pub fn merge_response(
    trace_id: &str,
    cached_responses: &mut Vec<Response>,
    search_responses: &mut Vec<Response>,
    ts_column: &str,
    limit: i64,
    is_descending: bool,
    cache_took: usize,
    order_by: Vec<(String, OrderBy)>,
) -> Response

// 排序响应
fn sort_response(
    is_descending: bool,
    cache_response: &mut Response,
    ts_column: &str,
    in_order_by: &Vec<(String, OrderBy)>,
)
```

### 4.3 数据结构关系

```
Request (config/meta/search.rs)
├── Query
│   ├── sql: String           ← 包含ORDER BY, LIMIT, OFFSET
│   ├── start_time: i64
│   ├── end_time: i64
│   ├── from: i64             ← 分页偏移
│   └── size: i64             ← 每页大小
├── use_cache: bool
└── ...

Sql (sql mod)
├── order_by: Vec<(String, OrderBy)>
├── histogram_interval: Option<i64>
├── limit: i64
└── ...

MultiCachedQueryResponse (common/meta/search.rs)
├── cached_response: Vec<CachedQueryResponse>
├── deltas: Vec<QueryDelta>
├── has_cached_data: bool
├── ts_column: String
├── is_descending: bool
├── order_by: Vec<(String, OrderBy)>
├── limit: i64
├── file_path: String
└── ...

CachedQueryResponse
├── cached_response: Response
├── response_start_time: i64
├── response_end_time: i64
└── ...
```

---

## 五、关键结论汇总

### 5.1 ORDER BY 与哈希计算

| 排序信息 | 是否进入hashed_query | 是否影响文件名 |
|---------|---------------------|---------------|
| 完整ORDER BY子句（作为SQL的一部分） | ✅ 是 | ❌ 否 |
| 排序方向（is_descending） | ✅ 隐含在SQL中 | ✅ 是（文件名后缀） |
| 非时序排序列 | ✅ 隐含在SQL中 | ❌ 否 |
| LIMIT / OFFSET | ✅ 隐含在SQL中 | ❌ 否 |

**设计意图**:
- SQL字符串包含所有查询语义，保证语义不同的查询使用不同缓存
- is_descending作为文件名后缀，用于快速筛选和delta计算
- 不同排序方向的SQL本身就不同，哈希自然不同，目录也不同

### 5.2 时间窗变化的影响

- **哈希键不变**：相同SQL的不同时间窗查询使用同一缓存目录
- **文件名变化**：每个时间窗有独立的缓存文件
- **增量更新**：通过delta计算只查询未覆盖的时间范围
- **缓存失效**：基于stream_min_ts被动失效，或主动删除过期缓存

### 5.3 from > 0 绕过缓存的一致性

- **根本原因**：SQL中的OFFSET导致哈希不同，翻页查询天然与首页缓存隔离
- **排序一致**：首页和翻页使用相同的ORDER BY解析和排序逻辑
- **数据一致**：不可变数据 + delta边界+1微秒，避免重复和遗漏
- **权衡**：翻页性能较差，但保证了分页正确性，符合"首页快，翻页准"的设计目标
