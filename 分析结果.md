# 查询结果缓存与分页协作机制深度分析

## 一、WHERE条件、函数转换和排序参数的哈希计算与规范化

### 1.1 哈希计算的核心维度

在 `prepare_cache_response` 函数中 (`cache/mod.rs:574-593`)，哈希计算包含以下维度：

```rust
let mut hash_body = vec![
    CACHE_VERSION.to_string(),      // 缓存版本 "v3"
    origin_sql.to_string(),         // 规范化后的SQL语句（包含WHERE条件）
    req.query.size.to_string(),     // 分页大小（每页记录数）
];
if let Some(vrl_function) = &query_fn {
    hash_body.push(vrl_function.to_string());  // VRL函数转换
}
if let Some(action_id) = action {
    hash_body.push(action_id.to_string());
}
if !req.regions.is_empty() {
    hash_body.extend(req.regions.clone());
}
if !req.clusters.is_empty() {
    hash_body.extend(req.clusters.clone());
}
```

#### 1.1.1 WHERE条件的参与方式

**WHERE条件是通过 `origin_sql` 字符串直接参与哈希计算的**。因为SQL语句中包含完整的WHERE子句，任何WHERE条件的变化都会导致SQL字符串不同，从而产生不同的哈希值。

**示例说明**:
- 查询A: `SELECT * FROM logs WHERE level = 'error' LIMIT 100`
- 查询B: `SELECT * FROM logs WHERE level = 'warn' LIMIT 100`

这两个查询由于WHERE条件不同，`origin_sql` 字符串不同，哈希值不同，使用不同的缓存键。

#### 1.1.2 函数转换（VRL）的参与方式

VRL函数是通过 `query_fn` 单独加入哈希计算的：

```rust
let mut query_fn = req
    .query
    .query_fn
    .as_ref()
    .map(|v| match base64::decode_url(v) {
        Ok(v) => v,
        Err(_) => v.to_string(),
    });
// ...
if let Some(vrl_function) = &query_fn {
    hash_body.push(vrl_function.to_string());
}
```

**设计原因**:
- VRL函数是查询后对结果的转换逻辑
- 相同SQL + 不同VRL = 不同结果，需要不同缓存
- VRL函数通过base64编码传输，解码后才参与哈希计算

**特殊标记处理**:
```rust
let is_result_array_skip_vrl = query_fn
    .as_ref()
    .map(|v| is_result_array_skip_vrl(v))
    .unwrap_or(false);
if is_result_array_skip_vrl {
    query_fn = None;  // 特殊标记的VRL不参与缓存（在结果合并后应用）
}
```

#### 1.1.3 排序参数的特殊处理方式

**关键点：ORDER BY 不直接参与哈希计算！**

排序参数通过以下方式影响缓存，而不是直接加入哈希：

**方式1：缓存文件名后缀** (`cache/cacher.rs:452-458`)
```rust
let file_name = format!(
    "{}_{}_{}_{}.json",
    matching_meta.start_time,
    matching_meta.end_time,
    if cache_req.is_aggregate { 1 } else { 0 },
    if cache_req.is_descending { 1 } else { 0 }  // 排序方向作为文件名后缀
);
```

**方式2：缓存元数据筛选** (`cache/cacher.rs:423-449`)
```rust
let mut matching_meta = match cache_metas
    .iter()
    .filter(|m| {
        // 1. 检查时间范围重叠
        // 2. 检查histogram间隔对齐
        m.start_time <= cache_req.q_end_time && m.end_time >= cache_req.q_start_time
    })
    .max_by_key(|result| select_cache_meta(result, &cache_req, &selection_strategy))
```

**方式3：时间列推断** (`cache/cacher.rs:626-678`)
```rust
pub fn get_ts_col_order_by(
    parsed_sql: &Sql,
    _ts_col: &str,
    _is_aggregate: bool,
) -> Option<(String, bool)> {
    // 从ORDER BY子句中推断时间列和排序方向
    for (field, order) in order_by {
        if is_timestamp_field(field, &result_ts_col) {
            is_descending = order == &OrderBy::Desc;
            break;
        }
    }
}
```

**设计意图分析**:
1. ORDER BY改变的是结果顺序，不改变结果集合本身
2. 相同结果集的不同排序可以复用缓存数据，只是在合并时重新排序
3. 通过文件名后缀区分升序/降序，因为排序方向影响增量查询的delta计算
4. Histogram查询的非时间列排序有特殊处理

### 1.2 Histogram查询的规范化

在计算哈希前，会对histogram查询进行规范化处理 (`cache/mod.rs:555-572`):

```rust
if is_aggregate && sql.histogram_interval.is_some() {
    let mut req_time_range = (req.query.start_time, req.query.end_time);
    if req_time_range.1 == 0 {
        req_time_range.1 = now_micros();  // "now" 转换为具体时间戳
    }
    // ...
    cacher::handle_histogram(&mut origin_sql, q_time_range, req.query.histogram_interval);
}
```

`handle_histogram` 函数的作用 (`cache/cacher.rs:910-948`):
```rust
pub fn handle_histogram(
    origin_sql: &mut String,
    q_time_range: Option<(i64, i64)>,
    histogram_interval: i64,
) {
    // 将 histogram(_timestamp, '10 second') 规范化
    // 确保不同写法但语义相同的histogram产生相同的SQL字符串
    let interval = if histogram_interval > 0 {
        format!("{histogram_interval} second")
    } else {
        // 根据时间范围自动计算合适的间隔
        generate_histogram_interval(q_time_range)
    };
    *origin_sql = origin_sql.replace(
        caps.get(0).unwrap().as_str(),
        &format!("histogram({field},'{interval}')"),
    );
}
```

**规范化的目的**:
- `histogram(_timestamp, 3600)` 和 `histogram(_timestamp, '1 hour')` 语义相同
- 规范化后确保相同语义的查询命中同一个缓存

---

## 二、时间窗变化时的缓存命中与失效机制

### 2.1 缓存键的两级设计

```
第一级（目录级）：{org_id}/{stream_type}/{stream_name}/{hashed_query}
   ↳ 由SQL、size、VRL等计算，相同查询条件共享目录

第二级（文件级）：{start_time}_{end_time}_{is_aggregate}_{is_descending}.json
   ↳ 每个时间窗一个独立文件
```

### 2.2 哈希键不变但文件名变化的缓存命中流程

当查询时间窗变化但SQL等其他条件不变时：

1. **哈希键不变** → 进入相同的缓存目录
2. **遍历该目录下所有缓存文件** → 检查每个文件的时间范围
3. **筛选重叠的缓存** → 只保留与查询时间有交集的缓存
4. **选择最优缓存** → 按选择策略（Overlap/Duration/Both）打分

**筛选逻辑** (`cache/cacher.rs:423-449`):
```rust
.filter(|m| {
    // 1. 检查时间范围是否有交集
    // cache:  [10:00 ---------- 11:00]
    // query:      [10:30 ---------- 11:30]
    // 有交集 = cache.start <= query.end AND cache.end >= query.start
    m.start_time <= cache_req.q_end_time && m.end_time >= cache_req.q_start_time
    
    // 2. Histogram查询额外检查：时间边界必须对齐间隔
    if cache_req.is_aggregate && cache_req.histogram_interval > 0 {
        // 必须是间隔的整数倍
        m.start_time % cache_req.histogram_interval == 0
        && m.end_time % cache_req.histogram_interval == 0
    }
})
```

### 2.3 缓存命中后的二次过滤

即使缓存文件被选中，其数据可能超出查询时间范围，需要二次过滤：

```rust
// filter data based on request time range
if data_start_time < cache_req.q_start_time || data_end_time > cache_req.q_end_time {
    cached_response.hits.retain(|hit| {
        let hit_ts = get_ts_value(&cache_req.ts_column, hit);
        // 只保留查询时间范围内的数据
        hit_ts + histogram_interval < hits_allowed_end_time 
        && hit_ts >= hits_allowed_start_time
    });
    // 更新元数据为实际数据范围
    matching_meta.start_time = std::cmp::min(first_ts, last_ts);
    matching_meta.end_time = std::cmp::max(first_ts, last_ts) + histogram_interval;
}
```

**示例说明**:
```
查询: [10:30 - 11:30]
缓存文件: [10:00 - 11:00]  (实际数据包含 10:00-10:59)

二次过滤后:
实际使用数据: [10:30 - 10:59]
```

### 2.4 时间窗扩大/缩小时的缓存失效

#### 2.4.1 时间窗扩大
```
查询1: [10:00 - 11:00] → 缓存文件A: [10:00-11:00]
查询2: [10:00 - 12:00] → 时间窗扩大

处理流程:
1. 哈希键相同，找到缓存A
2. 缓存A覆盖 [10:00-11:00]
3. 计算delta: [11:00 - 12:00] (注意+1微秒避免重复)
4. 并行查询delta，合并结果
```

#### 2.4.2 时间窗缩小
```
查询1: [10:00 - 12:00] → 缓存文件B: [10:00-12:00]
查询2: [10:30 - 11:30] → 时间窗缩小

处理流程:
1. 哈希键相同，找到缓存B
2. 二次过滤，只保留 [10:30-11:30] 的数据
3. 无需额外查询，直接返回过滤后的缓存
```

#### 2.4.3 时间窗平移
```
查询1: [10:00 - 11:00] → 缓存C: [10:00-11:00]
查询2: [11:00 - 12:00] → 完全不重叠

处理流程:
1. 哈希键相同，遍历缓存目录
2. 缓存C时间范围 [10:00-11:00] 与查询 [11:00-12:00] 
   → 11:00 <= 12:00 AND 11:00 >= 11:00 = 边界重叠?
3. 实际判断: 相邻但不重叠时，重叠时间为0，可能不命中
4. 结果: 完全不命中，需要全新查询
```

### 2.5 基于stream_min_ts的被动失效

即使缓存文件存在，如果数据流中最早的数据已经被清理，缓存也会被标记为失效：

```rust
pub async fn invalidate_cached_response_by_stream_min_ts(
    file_path: &str,
    responses: &[CachedQueryResponse],
    histogram_interval: i64,
) -> Result<Vec<CachedQueryResponse>, String> {
    let stream_min_ts = infra::cache::stats::get_stream_stats(
        org_id, stream_name, stream_type
    ).doc_time_min;  // 流中现存数据的最早时间戳

    if histogram_interval > 0 {
        // Histogram查询: 只删除完全过期的bucket
        // bucket [10:00-11:00] 完全过期 <-> 11:00 <= stream_min_ts
        meta.cached_response.hits.retain(|hit| {
            let hit_ts = get_ts_value(&meta.ts_column, hit);
            hit_ts + histogram_interval > stream_min_ts
        });
    } else {
        // 普通查询: 修剪响应的开始时间
        responses
            .iter()
            .filter(|meta| meta.response_end_time >= stream_min_ts)
            .map(|mut meta| {
                if meta.response_start_time < stream_min_ts {
                    meta.response_start_time = stream_min_ts;
                }
                meta
            })
    }
}
```

**设计原因**:
- OpenObserve有数据保留策略（如保留最近30天）
- 缓存中可能包含已被物理删除的数据
- 通过 `stream_min_ts` 确保不返回已不存在的数据

---

## 三、from>0绕过缓存与delta增量查询的去重机制

### 3.1 from>0绕过缓存的设计原因

```rust
let use_cache = if in_req.query.from == 0 {
    in_req.use_cache
} else {
    false  // from > 0 时完全绕过缓存
};
```

**为什么翻页要绕过缓存？**

| 问题 | 原因分析 |
|------|----------|
| **数据一致性** | 缓存是快照，实时数据可能已变化。第2页用实时查询避免与第1页重复或遗漏 |
| **排序稳定性** | 相同时间戳的多条记录，排序顺序可能不稳定。翻页用相同数据源保证一致 |
| **边界精确** | 缓存+delta的边界是时间维度，而分页偏移是记录维度。两者难以精确对齐 |
| **实现复杂度** | 支持翻页缓存需要记录全局排序位置，大大增加复杂度 |

**翻页场景的实际行为**:
```
第1页 (from=0, size=100):
  ✓ 使用缓存 + delta增量查询
  → 结果: 记录1-100（缓存+实时合并）

第2页 (from=100, size=100):
  ✗ 完全绕过缓存，直接实时查询
  → 结果: 记录101-200（纯实时数据）

第3页 (from=200, size=100):
  ✗ 完全绕过缓存，直接实时查询
  → 结果: 记录201-300（纯实时数据）
```

### 3.2 delta增量查询避免重复的核心机制

当 `from=0` 时，缓存与delta并存，通过以下机制避免重复：

#### 3.2.1 时间边界 +1 微秒

**核心代码** (`cache/cacher.rs:534-548`):
```rust
if query_end_time > result_meta.end_time {
    // q end time : 11:00, r end time : 10:45
    // for delta start time we need to add 1 microsecond to the end time
    // because we will include the start_time, if we don't add 1 microsecond
    // the start_time will be include in the next search, we will get duplicate data
    let delta_start_time = if histogram_interval > 0 {
        result_meta.end_time      // Histogram: 不+1，按bucket边界对齐
    } else {
        result_meta.end_time + 1  // 普通查询: +1微秒避免边界重复
    };
    deltas.push(QueryDelta {
        delta_start_time,
        delta_end_time: query_end_time,
    });
}
```

**图解说明**:
```
缓存数据范围: [1000, 2000]  (包含时间戳1000到2000的所有记录)

普通查询（非Histogram）:
  查询范围: [1000, 3000]
  缓存覆盖: [1000, 2000]
  delta范围: [2001, 3000]  ← 注意+1了！
  
  记录A (ts=2000) → 在缓存中
  记录B (ts=2001) → 在delta中
  → 无间隙，无重复

Histogram查询:
  bucket间隔=100
  缓存覆盖: [1000, 2000]  (bucket 1000, 1100, ..., 1900)
  delta范围: [2000, 3000]  ← 不+1！
  
  bucket 2000: ts=2000-2099 → 在delta中
  bucket 1900: ts=1900-1999 → 在缓存中
  → 按bucket边界对齐，自然不重复
```

#### 3.2.2 Histogram查询的特殊处理

Histogram查询不使用+1微秒，原因：

1. **bucket对齐要求**: histogram数据是按时间间隔聚合的，必须从间隔起点开始
2. **bucket是原子的**: 一个bucket要么完整在缓存，要么完整在delta
3. **比较逻辑**: `hit_ts + histogram_interval > stream_min_ts`

**示例**:
```
histogram_interval = 3600秒（1小时）

缓存中的bucket:
- 10:00  → 覆盖 10:00:00 - 10:59:59
- 11:00  → 覆盖 11:00:00 - 11:59:59

delta查询从 11:00 开始（不加1）:
- 11:00  → 这个bucket会被重新查询吗？
  
  不会！因为:
  缓存中的 11:00 bucket 结束时间 = 11:00 + 3600 = 12:00
  delta.start_time = 11:00
  delta.start_time < 缓存.end_time (11:00 < 12:00)
  → 说明有重叠，这个缓存会被选中
  → 实际delta只会从 12:00 开始
```

#### 3.2.3 多缓存片段的协作去重

当有多个缓存片段时，`calculate_deltas_multi` 确保不重复：

```rust
fn calculate_deltas_multi(
    results: &[CachedQueryResponse],
    start_time: i64,
    end_time: i64,
    ...
) -> (Vec<QueryDelta>, Option<i64>, i64) {
    // 按结束时间降序排列
    let mut results = results.to_vec();
    results.sort_by(|a, b| b.response_end_time.cmp(&a.response_end_time));
    
    let mut current_end_time = end_time;
    for meta in results {
        // 计算当前缓存与上一个边界的gap
        calculate_deltas(
            &ResultCacheMeta { ... },
            current_start_time,
            current_end_time,
            ...
        );
        current_end_time = meta.response_start_time;  // 移动到当前缓存的开始
    }
    
    deltas.sort();      // 排序
    deltas.dedup();     // 去重
}
```

**多缓存场景示例**:
```
查询范围: [0, 1000]

现有缓存片段:
  缓存A: [600, 800]
  缓存B: [200, 400]

排序后（按end_time降序）:
  缓存A: [600, 800]  end=800
  缓存B: [200, 400]  end=400

delta计算过程:
  初始 current_end_time = 1000
  
  处理缓存A [600, 800]:
    delta = [801, 1000]  (A的结束到查询结束)
    current_end_time = 600
  
  处理缓存B [200, 400]:
    delta = [401, 600]  (B的结束到A的开始)
    current_end_time = 200
  
  最后检查开头:
    delta = [0, 200]  (查询开始到B的开始)

最终deltas: [0-200], [401-600], [801-1000]
→ 覆盖完整查询范围，无重叠，无间隙
```

### 3.3 结果合并时的排序与截断

即使delta计算时边界正确，合并时仍需最终确认：

```rust
pub fn merge_response(...) -> Response {
    // 1. 合并所有结果
    for res in cached_responses {
        resp.hits.extend(res.hits.clone());
    }
    for res in search_responses {
        resp.hits.extend(res.hits.clone());
    }
    
    // 2. 按ORDER BY重新排序
    sort_response(is_descending, &mut cache_response, ts_column, &order_by);
    
    // 3. 截断到limit数量
    if cache_response.hits.len() > (limit as usize) {
        cache_response.hits.truncate(limit as usize);
    }
}
```

**排序作为最后防线**:
- 即使delta边界计算有微小误差（如时间戳相同）
- 重新排序后相同时间戳的记录会排在一起
- 最终只取前limit条，重复记录最多影响结果顺序，不影响分页正确性

---

## 四、完整数据流对比

### 4.1 from=0（首页，启用缓存）
```
查询请求 (from=0, size=100)
    ↓
prepare_cache_response
    ├─ 解析SQL，提取WHERE条件
    ├─ 规范化histogram间隔
    ├─ 计算哈希键（SQL + size + VRL + ...）
    ├─ 推断ORDER BY中的时间列和排序方向
    └─ 构建缓存路径 org/type/stream/hash
    ↓
check_cache
    ├─ 遍历缓存目录下所有文件
    ├─ 筛选与查询时间重叠的缓存
    ├─ 按策略选择最优缓存（Overlap/Duration/Both）
    ├─ 基于stream_min_ts过滤过期缓存
    ├─ 对选中缓存进行二次过滤（裁剪到查询范围）
    └─ 计算delta时间范围（+1微秒避免重复）
    ↓
有delta需要查询?
    ├─ 是 → 并行执行delta查询
    └─ 否 → 跳过
    ↓
merge_response
    ├─ 合并缓存数据和delta查询结果
    ├─ 按ORDER BY重新排序
    └─ 截断到size条
    ↓
write_results（异步）
    ├─ 过滤不稳定数据（delay_secs之前的才缓存）
    ├─ 写入新缓存文件 start_end_agg_desc.json
    └─ 更新内存中的缓存元数据
    ↓
返回结果
```

### 4.2 from>0（翻页，绕过缓存）
```
查询请求 (from=100, size=100)
    ↓
use_cache = false （因为 from > 0）
    ↓
prepare_cache_response
    ├─ 仍然计算哈希键等元数据
    └─ 但不执行check_cache
    ↓
直接执行完整查询（不使用任何缓存）
    ↓
对查询结果排序
    ↓
跳过缓存写入（因为 use_cache=false）
    ↓
返回结果
```

---

## 五、关键设计权衡

| 设计决策 | 优点 | 缺点 | 适用场景 |
|---------|------|------|---------|
| **from>0绕过缓存** | 保证翻页一致性，实现简单 | 翻页性能较差 | 大多数场景下可接受，因为用户很少翻到很深的页 |
| **时间边界+1微秒** | 简单有效避免重复 | histogram需要特殊处理 | 普通日志查询 |
| **SQL字符串直接哈希** | WHERE条件变化自动隔离，实现简单 | SQL格式变化（空格、换行）导致缓存不命中 | 依赖调用方SQL规范化 |
| **ORDER BY不入哈希** | 不同排序可复用相同数据，缓存利用率高 | 文件名需要额外标记排序方向 | 大多数排序场景 |
| **两级缓存键设计** | 相同查询不同时间窗可部分命中，增量更新 | 目录下文件可能很多，遍历有开销 | 时间窗频繁变化的监控场景 |

---

## 六、核心代码位置索引

| 功能模块 | 文件位置 | 关键函数 |
|---------|---------|---------|
| 哈希计算 | `cache/mod.rs:574-593` | `prepare_cache_response` |
| delta计算 | `cache/cacher.rs:522-557` | `calculate_deltas` |
| 多缓存delta | `cache/cacher.rs:950-1005` | `calculate_deltas_multi` |
| 缓存筛选 | `cache/cacher.rs:423-449` | `get_cached_results` |
| 缓存选择策略 | `cache/cacher.rs:772-796` | `select_cache_meta` |
| 被动失效 | `cache/cacher.rs:46-112` | `invalidate_cached_response_by_stream_min_ts` |
| 结果合并 | `cache/mod.rs:667-828` | `merge_response` |
| histogram规范化 | `cache/cacher.rs:910-948` | `handle_histogram` |
| 缓存启用判断 | `cache/mod.rs:92-98` | 内联代码 |
