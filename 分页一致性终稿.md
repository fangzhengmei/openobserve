# 分页一致性终稿：query.from 与 SQL 文本的职责边界

## 一、query.from 与 SQL 文本的职责划分

### 1.1 数据模型中的分离

HTTP 请求到达后端时，`from` 和 SQL 是**独立字段**，不在同一个字符串中：

```rust
// config/src/meta/search.rs:110-148
pub struct Query {
    pub sql: String,         // ← 纯SQL，不含 OFFSET
    pub start_time: i64,
    pub end_time: i64,
    pub from: i64,           // ← 分页偏移，独立字段
    pub size: i64,           // ← 每页大小，独立字段
    // ...
}
```

**HTTP Handler 层的构造过程** (`handler/http/request/search/mod.rs:959-961`):

```rust
let mut query = config::meta::search::Query {
    sql: decoded_sql.clone(),     // ← SQL 字符串来自前端，不含 OFFSET
    from: req.from.unwrap_or(0),  // ← from 来自独立的请求参数
    size: req.size.unwrap_or(...),
    // ...
};
```

**关键事实**：前端传递给后端的 `sql` 字段中**不包含** `OFFSET from` 子句。`from` 作为独立的请求参数传递。

### 1.2 SQL 解析阶段：from → offset 存入结构体字段

`Sql::new_with_options` 中 (`sql/mod.rs:133-134`):

```rust
let sql = query.sql.clone();      // SQL 原文不修改
let offset = query.from as i64;   // from 转为 offset 存入 Sql 结构体
let mut limit = query.size as i64;
```

**Sql 结构体** (`sql/mod.rs:68-88`):

```rust
pub struct Sql {
    pub sql: String,               // ← SQL 文本，不含 OFFSET
    pub limit: i64,                // ← 来自 query.size
    pub offset: i64,               // ← 来自 query.from
    pub order_by: Vec<(String, OrderBy)>,
    // ...
}
```

**SQL 文本在解析过程中不会被注入 OFFSET**。`offset` 作为独立字段存储在 `Sql` 结构体中。

### 1.3 查询执行阶段：offset 通过优化规则注入 DataFusion

`offset` 不是拼接到 SQL 字符串中，而是通过 DataFusion 逻辑优化规则注入：

```rust
// datafusion/optimizer/mod.rs:89-136
pub fn generate_optimizer_rules(sql: &Sql) -> Vec<Arc<dyn OptimizerRule + Send + Sync>> {
    let limit = ...;
    let offset = sql.offset as usize;   // ← 来自 query.from

    // 通过优化规则注入 LIMIT + OFFSET
    if let Some(limit) = limit {
        rules.push(Arc::new(AddSortAndLimitRule::new(limit, offset)));
    };
}
```

`AddSortAndLimitRule` (`datafusion/optimizer/logical_optimizer/add_sort_and_limit.rs:28-63`):

```rust
pub struct AddSortAndLimitRule {
    limit: usize,
    offset: usize,    // ← 来自 query.from
}

impl OptimizerRule for AddSortAndLimitRule {
    fn rewrite(&self, plan: LogicalPlan, ...) -> Result<Transformed<LogicalPlan>> {
        // 在逻辑计划树中插入 Sort → Limit(offset+limit) → Offset(offset) 节点
        let mut plan = plan.rewrite(&mut AddSortAndLimit::new(self.limit, self.offset))?;
        // ...
    }
}
```

### 1.4 职责划分总结

| 职责 | 承载方式 | 阶段 |
|------|---------|------|
| **WHERE / ORDER BY / GROUP BY** | SQL 文本 `req.query.sql` | SQL 解析 → 逻辑计划 |
| **分页偏移 (from → offset)** | `Query.from` → `Sql.offset` → `AddSortAndLimitRule` | DataFusion 逻辑优化 |
| **每页大小 (size → limit)** | `Query.size` → `Sql.limit` → `AddSortAndLimitRule` | DataFusion 逻辑优化 |
| **时间范围** | `Query.start_time` / `end_time` | SQL 解析 → 文件筛选 + 逻辑计划 |

---

## 二、from=0 与 from>0 两条路径的差异与对齐点

### 2.1 完整路径对比

#### 阶段1：use_cache 门控

```rust
// cache/mod.rs:92-98
let use_cache = if in_req.query.from == 0 {
    in_req.use_cache          // from=0: 尊重请求的 use_cache 参数
} else {
    false                     // from>0: 强制禁用缓存
};
```

| | from=0 | from>0 |
|--|--------|--------|
| use_cache | 由请求参数决定 | 强制 `false` |
| 代码行 | `cache/mod.rs:94-95` | `cache/mod.rs:97` |

#### 阶段2：prepare_cache_response 分支

```rust
// cache/mod.rs:633-659
let resp = if use_cache {
    // from=0 路径: 调用 check_cache
    check_cache(
        trace_id, org_id, req, &mut origin_sql, &file_path,
        is_aggregate, &sql, &ts_column, is_descending,
        &mut should_exec_query,
    ).await
} else {
    // from>0 路径: 只返回解析元数据
    MultiCachedQueryResponse {
        ts_column,
        is_aggregate,
        is_descending,
        order_by: sql.order_by,
        limit: sql.limit,
        file_path,
        ..Default::default()   // cached_response=[], deltas=[], has_cached_data=false, cache_query_response=false
    }
};
```

| 字段 | from=0 (经 check_cache) | from>0 (Default 覆盖) |
|------|------------------------|----------------------|
| `cached_response` | 有缓存的命中结果 | `[]` 空 |
| `deltas` | 计算出的增量时间范围 | `[]` 空 |
| `has_cached_data` | `true`（如果命中缓存） | `false` |
| `cache_query_response` | `true`（在 check_cache 内设置） | `false` |
| `ts_column` | ✅ 相同 | ✅ 相同 |
| `is_descending` | ✅ 相同 | ✅ 相同 |
| `order_by` | ✅ 相同 | ✅ 相同 |
| `limit` | ✅ 相同 | ✅ 相同 |
| `file_path` | ✅ 相同 | ✅ 相同 |

**对齐点**：两个路径解析出**完全相同的元数据**（ts_column、is_descending、order_by、limit、file_path），差异仅在于缓存查询结果的有无。

#### 阶段3：delta 查询

```rust
// cache/mod.rs:136-142
if !c_resp.has_cached_data && c_resp.deltas.is_empty() {
    // from>0 必定进入此分支（has_cached_data=false, deltas=[]）
    c_resp.deltas.push(QueryDelta {
        delta_start_time: req.query.start_time,
        delta_end_time: req.query.end_time,
    });
}
```

| | from=0 | from>0 |
|--|--------|--------|
| delta 来源 | check_cache 计算的增量范围 | 整个查询时间范围 `[start_time, end_time]` |
| delta 数量 | 0（完全命中）或多个增量片段 | 恰好1个 |
| 实际查询范围 | 只查询缓存未覆盖的部分 | 查询完整时间范围 |
| 代码行 | `cache/mod.rs:227-268` | `cache/mod.rs:137-141` |

#### 阶段4：delta 查询执行

两个路径的执行代码完全相同 (`cache/mod.rs:227-268`):

```rust
for (i, delta) in c_resp.deltas.into_iter().enumerate() {
    let mut req = req.clone();       // ← 注意: req 包含原始的 query.from
    req.query.start_time = delta.delta_start_time;
    req.query.end_time = delta.delta_end_time;
    // req.query.from 和 req.query.size 保持不变
    SearchService::search(&trace_id, &org_id, stream_type, user_id, &req).await
}
```

**关键**：delta 查询中 `req.query.from` 和 `req.query.size` **保持不变**。

- **from=0**：`req.query.from = 0`，delta 查询从偏移0开始
- **from>0**：`req.query.from = 100`（或其他值），delta 查询也从偏移100开始

**这意味着底层查询引擎在两种路径下收到的 from/size 参数完全一致**，结果集的偏移逻辑由 DataFusion 的 `AddSortAndLimitRule` 统一处理。

#### 阶段5：结果合并与排序

**from=0 路径**（有缓存数据时）(`cache/mod.rs:273-287`):

```rust
if c_resp.has_cached_data {
    merge_response(
        trace_id,
        &mut c_resp.cached_response.iter().map(|r| r.cached_response.clone()).collect(),
        &mut results,            // delta 查询结果
        &c_resp.ts_column,
        c_resp.limit,
        c_resp.is_descending,
        c_resp.took,
        c_resp.order_by,
    )
}
```

**from=0 路径**（无缓存数据时）(`cache/mod.rs:288-297`):

```rust
else {
    let mut reps = results[0].clone();
    sort_response(
        c_resp.is_descending,
        &mut reps,
        &c_resp.ts_column,
        &c_resp.order_by,
    );
    reps
}
```

**from>0 路径**：`has_cached_data = false`，必定走 `else` 分支，与 from=0 无缓存数据的路径完全一致。

**sort_response 的逻辑** (`cache/mod.rs:830-899`):

```rust
fn sort_response(is_descending: bool, cache_response: &mut Response, ts_column: &str, in_order_by: &Vec<(String, OrderBy)>) {
    let order_by = if in_order_by.is_empty() {
        // 无 ORDER BY 时退化为时间列排序
        &vec![(ts_column.to_string(), if is_descending { Desc } else { Asc })]
    } else {
        in_order_by
    };
    cache_response.hits.sort_by(|a, b| {
        for (field, order) in order_by {
            // 按字段逐一比较，支持多列排序
        }
    });
}
```

**对齐点**：两条路径使用**完全相同的排序逻辑和排序参数**。

#### 阶段6：缓存写入

```rust
// cache/mod.rs:435-442
let should_cache_results = cfg.common.result_cache_enabled
    && !is_http2_streaming
    && should_exec_query
    && c_resp.cache_query_response       // ← from>0 时为 false
    && res.new_start_time.is_none()
    && res.new_end_time.is_none()
    && res.function_error.is_empty()
    && !res.hits.is_empty();
```

| | from=0 | from>0 |
|--|--------|--------|
| `c_resp.cache_query_response` | `true` | `false` |
| 是否写入缓存 | ✅ 可能写入 | ❌ 永不写入 |

### 2.2 完整差异对照表

| 阶段 | from=0 | from>0 | 对齐点 |
|-----|--------|--------|--------|
| **use_cache门控** | 尊重请求参数 | 强制 `false` | — |
| **prepare_cache_response** | 调用 check_cache | 跳过 check_cache，返回元数据 | 相同的 ts_column/is_descending/order_by/limit/file_path |
| **delta生成** | 计算增量范围 | 整个时间范围 | — |
| **delta查询** | `req.query.from=0` | `req.query.from=原值` | 相同的SQL、size、排序 |
| **结果合并** | merge_response (缓存+delta) | sort_response (纯搜索结果) | 相同的排序逻辑和参数 |
| **缓存写入** | 可能写入 | 永不写入 | — |
| **DataFusion执行** | `AddSortAndLimitRule(limit, 0)` | `AddSortAndLimitRule(limit, from)` | 相同的SQL、优化规则 |

---

## 三、哈希键是否受 query.from 影响的结论与证据

### 3.1 明确结论

**哈希键不受 `query.from` 影响。**

无论 `from=0` 还是 `from>0`，只要 SQL 文本和 `size` 相同，计算出的 `hashed_query` 就相同。

### 3.2 证据链

#### 证据1：哈希计算的输入不包含 from

```rust
// cache/mod.rs:574-593
let mut hash_body = vec![
    CACHE_VERSION.to_string(),      // "v3"
    origin_sql.to_string(),         // SQL 文本（不含 OFFSET）
    req.query.size.to_string(),     // size
];
// + vrl_function (可选)
// + action_id (可选)
// + regions (可选)
// + clusters (可选)
let mut h = config::utils::hash::gxhash::new();
let hashed_query = h.sum64(&hash_body.join(","));
```

**遍历 hash_body 的每个元素**：没有 `req.query.from`。

#### 证据2：origin_sql 取自 req.query.sql，不含 OFFSET

```rust
// cache/mod.rs:505
let mut origin_sql = req.query.sql.clone();
```

`req.query.sql` 是前端传入的 SQL 文本，前端**不会**将 `from` 作为 `OFFSET` 子句拼接到 SQL 中（见 1.1 节的 Handler 构造代码）。

#### 证据3：histogram 规范化修改 origin_sql，但不涉及 from

```rust
// cache/mod.rs:557-572
if is_aggregate && sql.histogram_interval.is_some() {
    // 修改 origin_sql 中的 histogram 间隔格式
    cacher::handle_histogram(&mut origin_sql, q_time_range, req.query.histogram_interval);
}
```

`handle_histogram` 只替换 histogram 函数的间隔参数，不涉及 `from`/`OFFSET`。

#### 证据4：相同 SQL + 不同 from → 相同哈希

```
查询1: sql="SELECT * FROM logs WHERE level='error' ORDER BY _timestamp DESC", size=100, from=0
  hash_body = ["v3", "SELECT * FROM logs ...", "100"]
  hashed_query = gxhash64("v3,SELECT * FROM logs ...,100") = X

查询2: sql="SELECT * FROM logs WHERE level='error' ORDER BY _timestamp DESC", size=100, from=100
  hash_body = ["v3", "SELECT * FROM logs ...", "100"]
  hashed_query = gxhash64("v3,SELECT * FROM logs ...,100") = X  ← 与查询1完全相同！
```

### 3.3 相同哈希的实际影响

**from=0 和 from>0 使用相同的缓存目录，但不会产生数据冲突**，原因如下：

1. **from=0 时写入缓存**：缓存包含 `[0, size)` 条记录
2. **from>0 时不读也不写缓存**：`use_cache=false`，既不读取也不写入
3. **两者不交叉**：from=0 路径独占缓存读写，from>0 路径完全绕过缓存

```
from=0 查询: 
  读取缓存 → 命中 → delta查询 → 合并 → 写入缓存
                    ↘ 未命中 → 完整查询 → 写入缓存

from>0 查询:
  跳过缓存 → 完整查询 → 跳过缓存写入
```

### 3.4 一致性保证的核心机制

**既然 from>0 的哈希与 from=0 相同且缓存目录相同，为什么翻页不会读到首页的缓存？**

因为 `use_cache=false` 在 `prepare_cache_response` 中直接导致 `check_cache` 不被调用：

```rust
// cache/mod.rs:633-659
let resp = if use_cache {
    check_cache(...)           // from=0: 读取缓存
} else {
    MultiCachedQueryResponse { // from>0: 不读缓存
        ..Default::default()
    }
};
```

同时 `cache_query_response=false` 导致不写入缓存：

```rust
// cache/mod.rs:438
&& c_resp.cache_query_response    // from>0 时为 false，跳过写入
```

**一致性机制总结**：

| 机制 | 作用 | 代码位置 |
|------|------|---------|
| **use_cache 门控** | from>0 完全不读取缓存 | `cache/mod.rs:92-98` |
| **cache_query_response=false** | from>0 完全不写入缓存 | `cache/mod.rs:649-658` (Default) |
| **SQL文本不含OFFSET** | from 不污染哈希键，但也不影响缓存隔离 | `cache/mod.rs:505` |
| **相同的排序逻辑** | 两条路径结果排序一致 | `cache/mod.rs:830-899` |
| **DataFusion统一处理offset** | from>0 的偏移在查询引擎层实现 | `optimizer/mod.rs:101,135` |

### 3.5 from>0 绕过缓存后与首页结果的一致性分析

**问题**：首页使用缓存+delta，翻页纯实时查询，会不会出现数据重复或遗漏？

**分析**：

1. **首页返回的记录范围**：排序后取 `[0, size)` 条
   - 缓存数据：过去查询结果，已固化
   - delta数据：实时查询结果，最新数据
   - 合并后排序，截断到 size 条

2. **翻页返回的记录范围**：排序后取 `[from, from+size)` 条
   - 纯实时查询整个时间范围
   - DataFusion 的 `AddSortAndLimitRule` 先排序再跳过 from 条

3. **潜在风险**：

| 风险场景 | 可能性 | 说明 |
|---------|--------|------|
| 缓存数据与实时数据不一致 | 低 | 缓存只存 `cache_delay_secs` 之前的已固化数据，新数据在delta中实时查询 |
| 相同排序键值的记录顺序不稳定 | 中 | 如果多条记录排序键值相同，应用层排序和SQL引擎排序可能不同；但首页的 merge_response 会重新排序，使用相同的 order_by 参数 |
| 翻页期间新数据写入 | 低 | 新数据时间戳 > cache_delay_secs，不会出现在历史查询的已缓存范围内 |
| 缓存的部分数据被retention清理 | 低 | check_cache 内调用 invalidate_cached_response_by_stream_min_ts 进行过滤 |

4. **最核心的保证**：

   - 首页的缓存是**过去某个时刻查询的快照**
   - delta 查询补充了缓存之后的新数据
   - 两者合并后覆盖完整时间范围
   - 翻页查询同样覆盖完整时间范围
   - 两者的区别仅在数据新鲜度：缓存部分是历史快照，delta部分是实时查询
   - 由于缓存写入时有 `cache_delay_secs` 延迟，缓存中的数据早已固化，不会变化

---

## 四、核心代码索引

| 关注点 | 文件 | 行号 | 关键代码 |
|--------|------|------|---------|
| from 与 sql 分离 | `config/src/meta/search.rs` | 110-148 | `Query { sql, from, size, ... }` |
| Handler 构造 Query | `handler/.../search/mod.rs` | 959-961 | `sql: decoded_sql, from: req.from` |
| from → offset | `service/search/sql/mod.rs` | 134 | `let offset = query.from as i64;` |
| offset 注入优化规则 | `datafusion/optimizer/mod.rs` | 101,135 | `offset=sql.offset; AddSortAndLimitRule(limit, offset)` |
| use_cache 门控 | `service/search/cache/mod.rs` | 92-98 | `if from==0 { use_cache } else { false }` |
| 哈希计算（无 from） | `service/search/cache/mod.rs` | 574-593 | `hash_body = [version, sql, size, ...]` |
| prepare_cache 分支 | `service/search/cache/mod.rs` | 633-659 | `if use_cache { check_cache } else { Default }` |
| delta 补全 | `service/search/cache/mod.rs` | 136-142 | `if !has_cached_data && deltas.is_empty()` |
| 结果排序 | `service/search/cache/mod.rs` | 830-899 | `sort_response(is_descending, ..., order_by)` |
| 缓存写入控制 | `service/search/cache/mod.rs` | 435-442 | `c_resp.cache_query_response` |
